<%= form_with(model: staff, local: true) do |f| %>
  <% if staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(staff.errors.count, "error") %> prohibited this staff from being saved:</h2>

      <ul>
      <% staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name %>
    <%= f.text_field :name, value: f.object.name %>
  </div>
  <div>
    <%= f.label :email %>
    <%= f.text_field :email, value: f.object.email %>
  </div>
  <div>
    <%= f.label :role %>
    <%= f.select :role, Staff.roles.keys.to_a, value: f.object.role %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
